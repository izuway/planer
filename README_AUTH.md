# Инструкция по настройке Firebase Authentication

## Что было реализовано

✅ **Backend (Cloudflare Worker):**
- Firebase JWT верификация через `@hono/firebase-auth`
- Middleware для проверки авторизации и email verification
- Защищенные API endpoints
- Auth роуты для работы с пользователями

✅ **Frontend (React):**
- Компоненты Login и Signup
- Экран верификации email
- AuthContext для управления состоянием пользователя
- Автоматическое обновление токенов
- Утилиты для API запросов с авторизацией

✅ **Безопасность:**
- JWT токены хранятся в localStorage
- Обязательная верификация email для доступа к приложению
- Автоматическое обновление токенов каждые 55 минут
- CORS настроен корректно
- Защита всех API endpoints

## Предварительные требования

1. **Firebase проект** - создайте в [Firebase Console](https://console.firebase.google.com/)
2. **Cloudflare Workers** - аккаунт с возможностью создания KV namespace

## Шаг 1: Настройка Firebase

### 1.1. Включите Email/Password аутентификацию

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Перейдите в **Authentication** → **Sign-in method**
4. Нажмите на **Email/Password**
5. Включите оба переключателя
6. Нажмите **Save**

### 1.2. Настройте шаблон email для верификации

1. В Firebase Console перейдите в **Authentication** → **Templates**
2. Нажмите на **Email address verification**
3. Настройте шаблон по желанию
4. **Важно**: установите Action URL на: `https://planer.m-k-mendykhan.workers.dev/`
5. Сохраните изменения

### 1.3. Получите конфигурацию Firebase

1. Перейдите в **Project Settings** (значок шестеренки)
2. Прокрутите вниз до раздела "Your apps"
3. Нажмите на значок **Web** (</>) чтобы добавить веб-приложение
4. Зарегистрируйте приложение с именем (например, "Planer Web")
5. Скопируйте конфигурационные значения

### 1.4. Добавьте авторизованные домены

1. В Firebase Console перейдите в **Authentication** → **Settings** → **Authorized domains**
2. Добавьте следующие домены:
   - `localhost` (для локальной разработки)
   - `planer.m-k-mendykhan.workers.dev` (ваш продакшн домен)

## Шаг 2: Настройка переменных окружения

### 2.1. Локальная разработка

Создайте файл `.env` в корне проекта:

```env
VITE_FIREBASE_API_KEY=ваш_api_key
VITE_FIREBASE_AUTH_DOMAIN=ваш_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ваш_project_id
VITE_FIREBASE_STORAGE_BUCKET=ваш_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=ваш_messaging_sender_id
VITE_FIREBASE_APP_ID=ваш_app_id
```

Замените значения на реальные из вашей конфигурации Firebase.

### 2.2. Cloudflare Workers

Файл `wrangler.jsonc` уже настроен, но вам нужно:

1. Заменить `FIREBASE_PROJECT_ID` на ваш реальный project ID
2. Создать KV namespace (см. следующий шаг)

## Шаг 3: Создание KV Namespace

### 3.1. Создайте KV namespace

Выполните команду:

```bash
npx wrangler kv:namespace create "PUBLIC_JWK_CACHE_KV"
```

### 3.2. Обновите wrangler.jsonc

Скопируйте ID из вывода команды и замените `YOUR_KV_NAMESPACE_ID` в файле `wrangler.jsonc`:

```json
"kv_namespaces": [
  {
    "binding": "PUBLIC_JWK_CACHE_KV",
    "id": "ваш_реальный_kv_namespace_id"
  }
]
```

## Шаг 4: Запуск проекта

### 4.1. Локальная разработка

```bash
# Установите зависимости (если еще не установлены)
npm install

# Запустите dev сервер
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### 4.2. Деплой в продакшн

```bash
npm run deploy
```

Это скомпилирует проект и задеплоит его на Cloudflare Workers.

## Как это работает

### Поток аутентификации:

1. **Регистрация:**
   - Пользователь вводит email и пароль
   - Firebase создает аккаунт
   - Автоматически отправляется письмо с верификацией
   - Пользователь видит экран "Verify Your Email"

2. **Верификация email:**
   - Пользователь получает письмо от Firebase
   - Кликает на ссылку верификации в письме
   - Перенаправляется на `https://planer.m-k-mendykhan.workers.dev/`
   - Кликает кнопку "I've Verified My Email"
   - Получает доступ к приложению

3. **Вход:**
   - Пользователь вводит email и пароль
   - Если email не верифицирован - показывается экран верификации
   - Если email верифицирован - получает полный доступ

4. **Защищенные запросы:**
   - Frontend автоматически добавляет JWT токен в заголовок `Authorization`
   - Backend проверяет токен через Firebase Auth
   - Проверяется статус верификации email
   - Если все ок - запрос выполняется
   - Если нет - возвращается ошибка 401 или 403

### Автоматическое обновление токенов:

- JWT токены Firebase действительны 1 час
- AuthContext автоматически обновляет токен каждые 55 минут
- Пользователь не замечает этого процесса

## API Endpoints

### Публичные (не требуют авторизации):

- `GET /health` - проверка здоровья сервера
- `POST /api/auth/verify` - верификация токена
- `GET /api/auth/email-status` - статус верификации email

### Защищенные (требуют авторизацию и верифицированный email):

- `GET /api/auth/me` - получить информацию о текущем пользователе
- `GET /api/versions` - получить версии приложения
- `GET /api/test` - тестовый endpoint

## Пример использования API

```typescript
import { get, post } from './utils/api';

// GET запрос
const versions = await get('/versions');

// POST запрос
const result = await post('/auth/verify', { token: 'xxx' });
```

Токен авторизации добавляется автоматически!

## Тестирование

1. Запустите проект локально: `npm run dev`
2. Откройте браузер на `http://localhost:5173`
3. Нажмите "Sign up"
4. Введите тестовый email и пароль
5. Проверьте почту и кликните на ссылку верификации
6. Вернитесь в приложение и нажмите "I've Verified My Email"
7. Вы должны увидеть главный экран приложения

## Возможные проблемы

### Ошибка "Invalid token"

**Причины:**
- Неправильная конфигурация Firebase
- Токен не отправляется в заголовке
- Токен истек (более 1 часа)

**Решение:**
- Проверьте конфигурацию Firebase в `.env`
- Убедитесь что `FIREBASE_PROJECT_ID` в `wrangler.jsonc` правильный
- Перезайдите в приложение

### Ошибка "Email not verified"

**Причины:**
- Email действительно не верифицирован
- Пользователь не кликнул на ссылку в письме

**Решение:**
- Проверьте папку спам
- Нажмите "Resend Verification Email"
- После клика на ссылку нажмите "I've Verified My Email"

### KV namespace ошибки

**Причины:**
- KV namespace не создан
- Неправильный ID в `wrangler.jsonc`

**Решение:**
- Создайте KV namespace: `npx wrangler kv:namespace create "PUBLIC_JWK_CACHE_KV"`
- Обновите ID в `wrangler.jsonc`

### CORS ошибки

**Причины:**
- Домен не добавлен в Firebase authorized domains

**Решение:**
- Добавьте ваш домен в Firebase Console → Authentication → Settings → Authorized domains

## Безопасность

⚠️ **Важные моменты:**

- ❌ **НЕ коммитьте** файл `.env` в git
- ✅ Используйте `.env.example` как шаблон
- ✅ API ключи Firebase можно использовать на клиенте (они предназначены для этого)
- ✅ Токены хранятся в localStorage (безопасно для SPA приложений)
- ✅ Токены автоматически обновляются
- ✅ Все защищенные endpoints требуют верифицированный email

## Структура проекта

```
planer/
├── src/
│   ├── components/
│   │   ├── Login.tsx          # Компонент входа
│   │   ├── Signup.tsx         # Компонент регистрации
│   │   └── EmailVerification.tsx  # Экран верификации email
│   ├── contexts/
│   │   └── AuthContext.tsx    # Контекст авторизации
│   ├── utils/
│   │   └── api.ts            # Утилиты для API запросов
│   ├── firebase.ts           # Конфигурация Firebase
│   ├── App.tsx              # Главный компонент
│   └── main.tsx             # Точка входа
├── worker/
│   ├── routes/
│   │   ├── auth.ts          # Auth роуты
│   │   └── versions.ts      # Версии приложения
│   ├── middleware/
│   │   └── index.ts         # Middleware (requireAuth, optionalAuth)
│   ├── types.ts            # TypeScript типы
│   └── index.ts            # Главный файл Worker
├── wrangler.jsonc          # Конфигурация Cloudflare Worker
├── .env                    # Переменные окружения (НЕ коммитить!)
├── .env.example           # Пример переменных окружения
└── README_AUTH.md         # Эта инструкция
```

## Следующие шаги

После успешной настройки авторизации вы можете:

1. Добавить дополнительные защищенные роуты
2. Реализовать восстановление пароля
3. Добавить профиль пользователя
4. Интегрировать с вашей базой данных D1
5. Добавить роли и права доступа

## Поддержка

Если возникли проблемы:

1. Проверьте логи в консоли браузера
2. Проверьте логи Cloudflare Worker в dashboard
3. Убедитесь что все переменные окружения настроены правильно
4. Проверьте что Firebase проект настроен корректно

---

**Готово!** 🎉 Теперь у вас есть полноценная система авторизации с обязательной верификацией email!

